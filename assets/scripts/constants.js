// 全局变量

// 大小
// 跳板上的红色方块
const redLandSize = {
    width: 10,
    height: 10,
};

const heroSize = {
    // width:
    // 英雄的高度
    height: 300,
};

module.exports = {
    heroSize,
    redLandSize,
}